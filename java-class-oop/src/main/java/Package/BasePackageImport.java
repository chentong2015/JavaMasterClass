package Package;

// TODO. 类型置于非正确package路径下仍然可以通过编译
// 但引用的其它类型则必须使用全路径引入
public class BasePackageImport {

    public BasePackageImport() {
        System.out.println("test java package import");
    }
}
