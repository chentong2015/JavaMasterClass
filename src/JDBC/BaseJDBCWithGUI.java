package JDBC;

/**
 * 对于有用户界面的Application, 需要解决以下问题
 * 1. 应用中的classes如何access data source
 * 2. 如何在执行数据库操作的时候，避免UI阻塞
 * 3. 如何将数据结果返回给用户
 */
public class BaseJDBCWithGUI {

    /**
     * 解决方案 1. TODO: 对于Datasource应该使用Singleton pattern单列, 只创建一个DB的instance
     * 1. private constructor
     * 2.
     *
     */
}
