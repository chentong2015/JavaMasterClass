package JavaBasicOOP.BaseModifier;

// 1. 当前类型没有至于正确的package路径下仍然可以通过编译
// 2. 如果要在别的路径使用当前类型，则必须引入正确的全路径
public class JavaPackageImport {

    public JavaPackageImport() {
        System.out.println("test java package import");
    }
}
