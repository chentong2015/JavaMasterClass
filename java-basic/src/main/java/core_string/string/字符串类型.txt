TODO. 不要使用String来代替基本类型
String模仿基本类型的行为(独立操作值)，但本身是引用类型(A Class)

String字符串本质上是一个16 bits unicode字符码值的数组
String对应Unicode码值，表示UTF-16编码方案

String字符串具有不可变性(Immutable)
- 如果要改变, 则需要创建新String对象 !!
- 直接String Concatenation级联造成巨大的时间复杂度 !!